<ion-view title="Photos">
  <ion-content class="has-header">
    <ion-list>
      <ion-item ng-repeat="(photoId, photo) in photos" type="item-text-wrap" href="#/tab/photo/{{photo.id}}">
        {{photoId}}
      </ion-item>
    </ion-list>

  </ion-content>
</ion-view>
